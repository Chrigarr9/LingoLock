---
phase: 03-notifications-live-activities
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - app/settings.tsx
  - app/challenge.tsx
  - app/index.tsx
  - src/services/widgetService.ts
autonomous: false

must_haves:
  truths:
    - "User can configure notification interval in settings (5/10/15/30 min options)"
    - "User can enable/disable notifications in settings"
    - "All practice contexts (in-app, notification, widget) share same FSRS queue"
    - "Widget updates after in-app and notification answers"
    - "Settings changes take effect immediately for notification scheduling"
  artifacts:
    - path: "app/settings.tsx"
      provides: "Notification interval and enable/disable settings controls"
      contains: "Notification"
  key_links:
    - from: "app/settings.tsx"
      to: "src/services/notificationScheduler.ts"
      via: "setNotificationInterval and enable/disable calls"
      pattern: "setNotificationInterval|saveNotificationsEnabled"
    - from: "app/challenge.tsx"
      to: "src/services/widgetService.ts"
      via: "updateWidgetData after session completion"
      pattern: "updateWidgetData"
    - from: "app/challenge.tsx"
      to: "src/services/notificationScheduler.ts"
      via: "pauseNotifications/resumeNotifications on mount/unmount"
      pattern: "pauseNotifications|resumeNotifications"
---

<objective>
Add notification configuration to the settings screen, wire widget updates into the challenge flow, and verify that all three practice contexts (in-app, notification, widget) share the same FSRS card queue and scheduling.

Purpose: Completes the notification and widget feature by adding user configuration and ensuring all practice contexts are unified. Without settings, users can't adjust notification frequency. Without cross-context wiring, answered cards may reappear.
Output: Settings screen with notification controls, widget refreshing after every practice context, and verified unified FSRS queue.
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-notifications-live-activities/03-CONTEXT.md
@.planning/phases/03-notifications-live-activities/03-01-SUMMARY.md
@.planning/phases/03-notifications-live-activities/03-02-SUMMARY.md
@.planning/phases/03-notifications-live-activities/03-03-SUMMARY.md
@app/settings.tsx
@app/challenge.tsx
@app/index.tsx
@src/services/notificationScheduler.ts
@src/services/widgetService.ts
@src/services/storage.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add notification settings to Settings screen</name>
  <files>app/settings.tsx</files>
  <action>
    Update the existing Settings screen to add notification configuration controls below the existing "New Words Per Day" stepper.

    Add new settings section (with separator between existing and new):

    1. **Notifications Enable/Disable Toggle:**
       - Label: "Vocabulary Notifications"
       - Subtitle: "Receive reminders when screen is unlocked"
       - Switch component (same style as Mute Audio toggle)
       - On change: call saveNotificationsEnabled(value) from storage
       - If enabling: call resumeNotifications() from scheduler, request permissions if not granted
       - If disabling: call pauseNotifications() and cancelAllNotifications() from scheduler
       - State initialized from loadNotificationsEnabled()
       - Platform guard: only show on native (Platform.OS !== 'web'), hide on web

    2. **Notification Interval Selector:**
       - Label: "Notification Interval"
       - Subtitle: "How often to send vocabulary reminders"
       - Stepper or segmented control with options: 5 min, 10 min, 15 min, 30 min
       - Use a row of Pressable buttons styled like a segmented control (matching app's glass card aesthetic)
       - Values: [300, 600, 900, 1800] seconds
       - Display labels: ["5 min", "10 min", "15 min", "30 min"]
       - On change: call saveNotificationInterval(seconds) from storage AND setNotificationInterval(seconds) from scheduler (immediate effect)
       - State initialized from loadNotificationInterval()
       - Only visible when notifications are enabled
       - Platform guard: only show on native

    Follow existing Settings screen patterns exactly:
    - Same card style (glassBackground, glassBorder)
    - Same typography (bodyLarge for labels, bodySmall for subtitles)
    - Same spacing and separator patterns
    - Import useAppTheme for consistent theming

    Import from storage: loadNotificationsEnabled, saveNotificationsEnabled, loadNotificationInterval, saveNotificationInterval
    Import from notificationScheduler: setNotificationInterval, pauseNotifications, resumeNotifications, cancelAllNotifications
    Import from notificationService: requestNotificationPermissions
  </action>
  <verify>
    - TypeScript compiles: `npx tsc --noEmit`
    - Settings screen renders without errors on web (notification controls hidden on web)
    - Notification toggle and interval selector are visible on native
    - Changing interval calls both storage save and scheduler update
    - Existing settings (mute, new words per day) still work
  </verify>
  <done>Settings screen has notification enable/disable toggle and interval selector (5/10/15/30 min). Changes take effect immediately. Controls hidden on web platform.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Notifications and Widget system:
    - Notification infrastructure with expo-notifications (categories, permissions, response handler)
    - Screen unlock detection via AppState timing heuristic
    - Notification scheduling service (configurable interval, daily limit, pause during practice)
    - Notification response handling (text input, FSRS update, streak management, feedback notifications)
    - Home Screen / Lock Screen widget with vocabulary card display and progress
    - Settings screen with notification interval and enable/disable controls
    - All practice contexts (in-app, notification, widget) share same FSRS queue
  </what-built>
  <how-to-verify>
    Since this requires a physical iOS device for full testing, verify what can be verified in development:

    1. **Build verification:**
       - Run `npx tsc --noEmit` -- should pass with no errors
       - Run `npm test` -- all existing tests should pass
       - Run `npx expo export --platform web` -- web build should succeed (notification/widget code hidden behind Platform guards)

    2. **Code review verification:**
       - Check app/settings.tsx: notification toggle and interval selector present
       - Check src/services/notificationService.ts: setupNotifications, processNotificationAnswer exist
       - Check src/services/notificationScheduler.ts: scheduleNextNotification, pauseNotifications exist
       - Check src/services/screenUnlockDetector.ts: startScreenUnlockDetection exists
       - Check widgets/VocabularyWidget.tsx (or equivalent): widget component exists
       - Check src/services/widgetService.ts: updateWidgetData exists

    3. **Integration verification:**
       - app/_layout.tsx calls setupNotifications()
       - app/challenge.tsx calls pauseNotifications/resumeNotifications and updateWidgetData
       - notificationService.ts processNotificationAnswer calls scheduleReview, updateStatsAfterSession
       - notificationScheduler.ts checks for due cards before scheduling

    4. **Settings UI (web browser):**
       - Open app on web
       - Navigate to Settings
       - Verify notification controls are NOT visible (web platform guard)
       - Verify existing settings (mute, new words per day) still work

    Type "approved" if everything looks correct, or describe any issues found.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm test` passes
- `npx expo export --platform web` succeeds
- Settings screen has notification controls (hidden on web)
- All notification and widget service files exist with correct exports
- Cross-context FSRS queue sharing verified through code review
</verification>

<success_criteria>
Notification interval configurable (5/10/15/30 min) in settings. Notifications can be enabled/disabled. Widget refreshes after in-app and notification answers. All practice contexts share FSRS queue. Web build succeeds with platform guards. Phase 03 functionality complete and ready for device testing.
</success_criteria>

<output>
After completion, create `.planning/phases/03-notifications-live-activities/03-04-SUMMARY.md`
</output>
