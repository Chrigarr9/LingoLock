---
phase: 02.2-app-polish-missing-screens
plan: 01
subsystem: ui
tags: [react-native, expo-router, mmkv, localstorage, settings, spaced-repetition]

# Dependency graph
requires:
  - phase: 02.1-pwa-deployment-content-integration
    provides: localStorage web adapter pattern (ll. prefix, synchronous API)
  - phase: 02-spaced-repetition-progress
    provides: buildSession, cardSelector, MMKV storage service

provides:
  - Settings screen at /settings with mute toggle and new-words-per-day stepper
  - loadNewWordsPerDay / saveNewWordsPerDay (MMKV + web adapters)
  - loadNewWordsIntroducedToday / recordNewWordsIntroduced (date-reset daily tracking)
  - buildSession respects daily new-word budget via loadNewWordsIntroducedToday
  - Routes registered: settings, vocabulary, stats, vocabulary/[id]
  - Route stubs: app/stats.tsx, app/vocabulary/[id].tsx

affects:
  - 02.2-02-PLAN (stats/vocabulary screens — routes now registered)
  - 02.2-03-PLAN (continuous sessions — must call recordNewWordsIntroduced at completion)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Daily tracking pattern: store date key + count key, reset count when date changes"
    - "MMKV number storage: statsStorage.getNumber(key) for integer preferences"
    - "Stepper pattern: IconButton minus/plus with Math.max/min clamping"

key-files:
  created:
    - app/settings.tsx
    - app/stats.tsx
    - app/vocabulary/[id].tsx
  modified:
    - src/services/storage.ts
    - src/services/storage.web.ts
    - src/services/cardSelector.ts
    - app/_layout.tsx

key-decisions:
  - "buildSession parameter renamed cardCount -> dailyNewWordBudget: due reviews now unlimited, new words capped by remaining daily budget"
  - "buildSession remains pure/idempotent: recordNewWordsIntroduced not called inside buildSession — caller (challenge.tsx) records at session completion"
  - "Due reviews now ALL included in session (no maxDue cap): continuous sessions review everything due"
  - "app/stats.tsx and app/vocabulary/[id].tsx created as route stubs to resolve Expo Router type errors and unblock future plan screens"

patterns-established:
  - "Settings screen pattern: SafeAreaView edges=['bottom'] (header provided by Stack.Screen title)"
  - "Stepper pattern: IconButton minus/plus with state clamping + disabled prop at limits"

requirements-completed: [POLISH-01, POLISH-02, POLISH-03]

# Metrics
duration: 3min
completed: 2026-03-03
---

# Phase 02.2 Plan 01: Settings Screen and Daily New-Word Cap Summary

**Settings screen with mute toggle and new-words-per-day stepper, daily budget enforcement in buildSession via MMKV + localStorage dual adapters**

## Performance

- **Duration:** 3 min
- **Started:** 2026-03-03T22:33:40Z
- **Completed:** 2026-03-03T22:36:52Z
- **Tasks:** 3
- **Files modified:** 7

## Accomplishments
- Added 4 new storage functions to both MMKV (storage.ts) and web (storage.web.ts) adapters with identical signatures
- Updated buildSession to use a dailyNewWordBudget parameter: due reviews unlimited, new words capped by remaining daily budget
- Created Settings screen with glass card wrapper, mute toggle (Switch), and new-words-per-day stepper (IconButton +/-)
- Registered all 4 new routes in _layout.tsx; created stubs for stats and vocabulary/[id] screens

## Task Commits

Each task was committed atomically:

1. **Task 1: Add new-words-per-day storage functions + daily tracking** - `f204154` (feat)
2. **Task 2: Update buildSession to accept dailyNewWordBudget** - `e6ea96a` (feat)
3. **Task 3: Create Settings screen + register all new routes** - `9d8bed5` (feat)

## Files Created/Modified
- `src/services/storage.ts` - Added loadNewWordsPerDay, saveNewWordsPerDay, loadNewWordsIntroducedToday, recordNewWordsIntroduced (MMKV)
- `src/services/storage.web.ts` - Parallel localStorage implementations with ll. prefix keys
- `src/services/cardSelector.ts` - buildSession rewritten with dailyNewWordBudget, imports loadNewWordsIntroducedToday
- `app/settings.tsx` - Settings screen: mute toggle + new-words-per-day stepper
- `app/stats.tsx` - Progress screen stub (for Plan 02.2-02)
- `app/vocabulary/[id].tsx` - Word detail stub (resolves Expo Router type error)
- `app/_layout.tsx` - Registered settings, vocabulary, stats, vocabulary/[id] routes

## Decisions Made
- buildSession parameter renamed `cardCount` -> `dailyNewWordBudget`: reflects the semantic change — it is now a daily cap on new words, not a fixed session card count. Due reviews are unlimited.
- buildSession remains pure: `recordNewWordsIntroduced` is NOT called inside buildSession. The caller (challenge.tsx, in Plan 02.2-03) records introduced words at session completion.
- Created `app/stats.tsx` and `app/vocabulary/[id].tsx` as minimal stubs. This was necessary to resolve a pre-existing Expo Router TypeScript error where `vocabulary.tsx` referenced `/vocabulary/[id]` which did not yet have a file, causing TS2820. Plan 02.2-02 will implement the full screens.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Created route stubs for stats and vocabulary/[id]**
- **Found during:** Task 3 (settings.tsx + route registration)
- **Issue:** Pre-existing TypeScript error TS2820 in vocabulary.tsx — route `/vocabulary/[id]` referenced but file didn't exist. Also needed for stats route.
- **Fix:** Created minimal stubs app/stats.tsx and app/vocabulary/[id].tsx with placeholder content
- **Files modified:** app/stats.tsx, app/vocabulary/[id].tsx
- **Verification:** `npx tsc --noEmit` compiles cleanly with zero errors
- **Committed in:** `9d8bed5` (Task 3 commit)

---

**Total deviations:** 1 auto-fixed (Rule 2 - missing critical route stubs)
**Impact on plan:** Stub creation was necessary for TypeScript correctness and unblocks Plan 02.2-02 which implements the full screens. No scope creep.

## Issues Encountered
- Pre-existing TS2820 error in vocabulary.tsx (created in an earlier session) referenced `/vocabulary/[id]` route that had no backing file. Resolved by creating a minimal stub in Task 3.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Settings screen ready at /settings route
- All 4 new routes registered — Plan 02.2-02 (stats/vocabulary screens) can proceed immediately
- Plan 02.2-03 (continuous sessions) must call `recordNewWordsIntroduced(newCardsCount)` in challenge.tsx at session completion to complete the daily cap feature end-to-end
- TypeScript compiles cleanly

---
*Phase: 02.2-app-polish-missing-screens*
*Completed: 2026-03-03*
